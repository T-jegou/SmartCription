export interface Database {
    public: {
      Tables: {
        userToken: {
          Row: {id: 'uuid',prescription:string, address: string} 
          Insert: {} 
          Update: {} 
        }
      }
    }
  }